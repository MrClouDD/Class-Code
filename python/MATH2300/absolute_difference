def calculate_difference(number: int) -> int:
    diff = abs(num - 21)
    if (number > 21):
        diff *= 2
    return diff

if __name__ == "__main__":
    num = 25
    print(f"There is an adjusted difference of {calculate_difference(num)} between {num} and 21")